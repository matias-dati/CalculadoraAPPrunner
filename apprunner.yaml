version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.8
  command: streamlit run app_modern.py --server.port 8080 --server.address 0.0.0.0
  network:
    port: 8080
    env: PORT